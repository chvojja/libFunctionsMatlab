function [tags,vals] = tablesCompare(T1,T2,tblQuest)
%TABLESCOMPARE Compares two tables T1 and T2
% takes a structure of tags with specified conditions on which to respond
% 

Ntags=numel(tblQuestT);

tags = [];
vals = [];

end

